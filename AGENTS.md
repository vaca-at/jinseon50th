# jinseon50th.com 작업 규칙

이 저장소를 고치는 에이전트(Claude Code, Antigravity 등)는 아래 규칙을 따릅니다.

## 시작 전
- 작업 전에 반드시 `git pull` 합니다. 여러 도구로 번갈아 고치기 때문에 GitHub에 새 커밋이 있을 수 있습니다.
- `index.html` = 진선 50주년 기념 웹앱, `gt/index.html` = 50주년 교직원 해외연수 웹앱.

## 관리자 로그인 (가장 중요)
- 관리자 화면은 **Firebase 이메일 로그인**으로 들어갑니다.
  - 계정: `anstjsdud@sen.go.kr` (센메일), 비밀번호는 숫자 6자리 — 비밀번호는 코드에 적지 않습니다.
- 앱별 암호나 해시 암호를 **새로 만들지 않습니다.**
- `#admin`으로 들어오면 관리자 대시보드(로그인 전이면 로그인 창)를 바로 엽니다. 이 동작을 지우지 않습니다 — moontr.site/admin 관리자 허브가 이 주소로 연결합니다.
- 새 앱(폴더)을 추가하면 **moontr 저장소**의 `admin/index.html` `GROUPS`에도 한 줄 추가합니다 (`login: 'pw'`).

## Firebase
- 프로젝트: `moon-d528d` (moontr.site와 함께 씁니다).
- 보안 규칙 파일은 이 저장소가 아니라 **moontr 저장소의 `firestore.rules`** 에 있습니다. 새 컬렉션을 쓰면 거기에 규칙을 추가하고, Firebase 콘솔에 게시해야 한다고 사용자에게 알려 줍니다.

## 마무리
- 코드 안 주석과 화면 문구는 한국어, 쉬운 말로 씁니다.
- 고친 뒤 커밋·푸시하고, 실제 사이트에서 확인할 주소를 알려 줍니다.
